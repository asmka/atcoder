#include <map>
#include <vector>
#include <string>
#include <iostream>

using namespace std;

int H, W, N;
int sr, sc;
string S;
string T;


int main() {
    cin >> H >> W >> N;
    cin >> sr >> sc;
    cin >> S;
    cin >> T;

    for (int i=W-1; i>=0; i--) {
        safe_zone[i]
        safe_zone[i] = 1;
    }

    // Left-Right
    int cc = sc;
    int maxRight;
    for (int i=0; i<N; i++) {

    }
}

