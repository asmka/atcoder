#include <iostream>
#include <vector>
#include <string>
using namespace std;

int main() {
    int N, X;
    cin >> N >> X;
    vector<int> blu {};
    for (int i=0; i<N; i++) {
        int b, l, u;
        cin >> b >> l >> u;
        blu.push_back();
    }
}
